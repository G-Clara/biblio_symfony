{% extends 'base.html.twig' %}

{% block title %}Livre index{% endblock %}

{% block body %}
    <h1>Livre index</h1>

        {% for livre in livres %}
            <a href="{{ path('app_livre_show', {'id': livre.id}) }}">
                <article class="bg-[#EEEEEE] hover:bg-[#5C9EAD] transition-colors duration-500 w-fit p-5 case">
                    <img src="{{ livre.photo }}" class=" w-60 h-60 flex items-end">
                        <h5 class="bg-[#5C9EAD] text-[#EEEEEE] w-36 py-2 mb-5 pr-2 text-end rounded-r-xl name_salle">{{ livre.nom }}</h5>
                    </img>
                </article>
            </a>
                    <a href="{{ path('app_livre_show', {'id': livre.id}) }}">show</a>
                    <a href="{{ path('app_livre_edit', {'id': livre.id}) }}">edit</a>
                
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_livre_new') }}">Create new</a>
{% endblock %}
