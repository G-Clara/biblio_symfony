{% extends 'base.html.twig' %}

{% block title %}Livre index{% endblock %}

{% block body %}
{# <h2>Prix</h2>
            <p><a href="livres_by_prix.php?prix=0">Prix croissant</a></p>
            <p><a href="livres_by_prix.php?prix=1">Prix décroissant</a></p>
        </div>

        <div class="filtre_groupe wrap">
            <h2>Langues</h2>{% for langue in langues %} <p> <a href="livres_by_langue.php?id_langue={{langue.id }}">{{langue.type}}</a></p>{% endfor %}
        </div>

        <div class="filtre_groupe">
            <h2>Auteurs</h2>{% for auteur in auteurs %} <p class="filtre_auteur"><a href="livres_by_auteur.php?id_auteur={{ auteur.id }}">{{auteur.prenom}} {{auteur.nom}}</a></p>{% endfor %} #}
    <h1>Livre index</h1>
    <div class="flex flex-wrap flex-row">
        {% for livre in livres %}
            <a href="{{ path('app_livre_show', {'id': livre.id}) }}">
                <article class="bg-[#f8edeb] hover:bg-[#f9dcc4] transition-colors duration-500 w-fit p-5 case mx-5">
                    <img src="{{ livre.photo }}" class=" w-60 h-60 flex items-end">
                        <h5 class="">{{ livre.nom }}</h5>
                        <p>{{ livre.prix }}</p>
                    </img>
                    <a href="{{ path('app_livre_show', {'id': livre.id}) }}">show</a>
                    <a href="{{ path('app_livre_edit', {'id': livre.id}) }}">edit</a>
                </article>
                <p>{{livre.auteur_id}}</p>

            </a>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        {# {% for livre in auteur.livre %}
        <p>{{ auteur.nom }}</p>
    {% endfor %} #}
    </div>

    <a href="{{ path('app_livre_new') }}">Create new</a>
{% endblock %}
